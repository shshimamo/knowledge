type User {
    id: ID!
    authUserId: ID!
    name: String
    createdAt: String!
    updatedAt: String!
}

type Query {
    getUser(id: ID!): User!
}

input NewUser {
  name: String!
}

type Mutation {
  createUser(input: NewUser!): User!
}
